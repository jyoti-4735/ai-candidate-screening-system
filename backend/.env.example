# Optional. If GEMINI_API_KEY is not set, the system runs in offline/template mode
# using TF-IDF retrieval + rule-based question construction (fully demoable, no cost).
GEMINI_API_KEY=
DATABASE_URL=sqlite:///./interview.db
